function gamesimulate(p1, p2, battle) {
    const result = null;
    while (battle) {
        p1_Hp = p1.playerData.hp - p2.playerData.attack;
        p2_Hp = p2.playerData.hp - p2.playerData.attack;
        if (p1_Hp <= 0 && p1_Hp <= 0) {
            break;
        }
    }
}